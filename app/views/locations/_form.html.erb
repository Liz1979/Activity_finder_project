<%= form_with(model: @location, local: true) do |form| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% end %>

<%= form_for @location do |f| %>
Name: <%= f.text_field :name %></br></br>
Country: <%= f.text_field :country %></br></br>
Longitude: <%= f.text_field :longitude %></br></br>
Latitide: <%= f.text_field :latitude %></br></br>
<%= f.submit %>
<% end %>